<UserControl x:Class="BudgetBuddy.Views.Helpers.MultiSelectComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetBuddy.Views.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">


            <UserControl.Resources>
                <SolidColorBrush x:Key="ControlBack" Color="#3A3F4B" />
                <SolidColorBrush x:Key="ControlBorder" Color="#5A6270" />
                <SolidColorBrush x:Key="AccentColor" Color="Green" />
                <SolidColorBrush x:Key="PopupBack" Color="#2F3440" />

                <Style x:Key="ChipStyle" TargetType="ContentControl">
                    <Setter Property="IsTabStop" Value="False" />
                    <Setter Property="Margin" Value="2,2,2,2" />
                    <Setter Property="MaxWidth" Value="300" />

            <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ContentControl">
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="ModernCheckBox" TargetType="CheckBox">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <StackPanel Orientation="Horizontal" Background="Transparent" Margin="2">
                                    <Border x:Name="box" 
                                    Width="18" Height="18" 
                                    CornerRadius="4" 
                                    BorderBrush="{StaticResource ControlBorder}" 
                                    BorderThickness="1" 
                                    Background="Transparent"
                                    Margin="0,0,10,0">
                                        <Path x:Name="check" 
                                      Data="M2,9 L6,13 L14,3" 
                                      Stroke="White" 
                                      StrokeThickness="2" 
                                      Visibility="Collapsed" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                                    </Border>
                                    <ContentPresenter VerticalAlignment="Center" />
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="box" Property="Background" Value="{StaticResource AccentColor}" />
                                        <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentColor}" />
                                        <Setter TargetName="check" Property="Visibility" Value="Visible" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentColor}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </UserControl.Resources>

            <Grid>
                <ToggleButton x:Name="MainToggle"
                      Height="Auto" MinHeight="36"
                      Background="{StaticResource ControlBack}"
                      BorderBrush="{StaticResource ControlBorder}"
                      BorderThickness="1">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                                <Grid Margin="4">
                                        <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Válassz!"
                                       Foreground="#A0A0A0"
                                       VerticalAlignment="Center"
                                       Margin="6,0,0,0"
                                       IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>


                                    <Path Grid.Column="1" 
                                  Data="M0,0 L5,5 L10,0" 
                                  Stroke="White" 
                                  StrokeThickness="1.5"
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" 
                                  Opacity="0.7"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <Popup IsOpen="{Binding IsChecked, ElementName=MainToggle}"
               StaysOpen="False"
               Placement="Bottom"
               PopupAnimation="Slide"
               AllowsTransparency="True"
               MinWidth="{Binding ActualWidth, ElementName=MainToggle}">

                    <Border Background="{StaticResource PopupBack}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    CornerRadius="8"
                    MaxHeight="250"
                    Margin="0,4,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="10,8" Background="Transparent" Cursor="Hand">
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Content="{Binding Name}"
                                              Style="{StaticResource ModernCheckBox}"
                                              Click="OnItemClick"/>

                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#3E4452"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </UserControl>

