<Window x:Class="BudgetBuddy.Views.Pages.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetBuddy.Views.Pages"
        xmlns:vm="clr-namespace:BudgetBuddy.ViewModels"
        mc:Ignorable="d"
        Title="Budget buddy"        
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        Height="500" Width="900">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <local:DashboardPage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataViewModel}">
            <local:DataPage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CategoriesViewModel}">
            <local:CategoriesPage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:IncomeViewModel}">
            <local:IncomePage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ImportViewModel}">
            <local:ImportPage />
        </DataTemplate>
    </Window.Resources>

    <Border Background="{StaticResource MainBlackBrush}" CornerRadius="18" Padding="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.ColumnSpan="2" Grid.Row="0" Height="60" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <Border Background="{StaticResource PanelBlackBrush}" CornerRadius="18"/>
                <TextBlock Text="Költég" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}" Margin="20,0,0,0" VerticalAlignment="Center"/>
                <Button Style="{StaticResource CloseButtonStyle}" Command="{Binding CloseApplicationCommand}"/>
            </Grid>

            <Border Grid.Column="0" Grid.Row="1" Background="{StaticResource PanelBlackBrush}" VerticalAlignment="Stretch" Margin="10" CornerRadius="12">
                <StackPanel Margin="10,0,10,0">
                    <Button Style="{StaticResource NavButtonStyle}" Content="📊 Statisztika" Command="{Binding NavigateToDashboardCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="📁 Adatok" Command="{Binding NavigateToDataCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="🏷️ Kategóriák" Command="{Binding NavigateToCategoriesCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="💰 Bevételek" Command="{Binding NavigateToIncomeCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="⬆️ Feltöltés" Command="{Binding NavigateToImportCommand}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Grid.Row="1" Margin="0,10,10,10" Background="#FF181818" CornerRadius="12">
                <ContentControl Content="{Binding CurrentViewModel}" Grid.Row="1"/>
            </Border>
        </Grid>
    </Border>
</Window>